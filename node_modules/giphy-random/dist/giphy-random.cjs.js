'use strict';

function _interopDefault (ex) { return (ex && (typeof ex === 'object') && 'default' in ex) ? ex['default'] : ex; }

var axios = _interopDefault(require('axios'));

/**
 * Get random gif from Giphy.
 *
 * @param {String} apiKey
 * @param {String} options.tag
 * @param {String} options.rating
 * @return {Object}
 */

const giphyRandom = async (apiKey, {
  tag,
  rating = "g"
} = {}) => {
  const params = {
    api_key: apiKey,
    rating
  };

  if (tag) {
    params.tag = tag;
  }

  const {
    data
  } = await axios.get("https://api.giphy.com/v1/gifs/random", {
    params
  });
  return data;
};

module.exports = giphyRandom;
